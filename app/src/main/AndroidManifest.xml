<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.e_vet">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission
        android:name="android.permission.ACCESS_FINE_LOCATION"
        tools:ignore="CoarseFineLocation"
        />
    <uses-permission android:name="andorid.permission.INTERNET" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ.EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EVet">
        <activity
            android:name=".hvermek2"
            android:exported="false" />
        <activity
            android:name=".hvermek"
            android:exported="false" />
        <activity
            android:name=".Sahip"
            android:exported="false" />
        <activity
            android:name=".HayvanAlmak"
            android:exported="false" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MappsActivity"
            android:label="@string/title_activity_mapps" />
        <activity android:name=".Genelilkyardim" />
        <activity android:name=".Ybancicisimyutma" />
        <activity android:name=".Yanik" />
        <activity android:name=".Elektrikcarpmasi" />
        <activity android:name=".Gidazehirlenmesi" />
        <activity android:name=".Arisokmasi" />
        <activity android:name=".Hayvanlaricinilkyardim" />
        <activity android:name=".Profile" />
        <activity android:name=".Profilbilgilerimiguncelle" />
        <activity android:name=".Sifredegistir" />
        <activity android:name=".Gostergepaneli" />
        <activity android:name=".Girisyap" />
        <activity android:name=".Kayitol" />
        <activity android:name=".Menu" />
        <activity android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat"/>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>